module("Splash Page");

test('Test setUpCarousel', function()
{
	strictEqual(setUpCarousel(), 0);
});

test('Test setUpDateTimePicker', function()
{
	strictEqual(setUpDateTimePicker(), 0);
});

test('Test fillCarouselContents', function()
{
	strictEqual(fillCarouselContents(), 0);
});